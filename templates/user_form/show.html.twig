{% extends 'base.html.twig' %}

{% block title %}UserForm{% endblock %}

{% block body %}
    <h1>UserForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_form.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_form.name }}</td>
            </tr>
            <tr>
                <th>User_name</th>
                <td>{{ user_form.userName }}</td>
            </tr>
            <tr>
                <th>Zip_code</th>
                <td>{{ user_form.zipCode }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_form.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user_form.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_form_index') }}">back to list</a>

    <a href="{{ path('user_form_edit', {'id': user_form.id}) }}">edit</a>

    {{ include('user_form/_delete_form.html.twig') }}
{% endblock %}
